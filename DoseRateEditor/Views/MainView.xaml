<metro:MetroWindow x:Class="DoseRateEditor.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DoseRateEditor.Views"
        xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls" 
        xmlns:ie="http://schemas.microsoft.com/xaml/behaviors" 
        xmlns:oxy1="clr-namespace:OxyPlot.Series;assembly=OxyPlot" 
        xmlns:oxy="http://oxyplot.org/wpf" 
        xmlns:viewmodels="clr-namespace:DoseRateEditor.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
                   mc:Ignorable="d"
        Title="{Binding AppTitle}" Height="850" Width="1600">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition>

            </ColumnDefinition>
            <ColumnDefinition>

            </ColumnDefinition>
            <ColumnDefinition>

            </ColumnDefinition>
            <ColumnDefinition>

            </ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="100*">

            </RowDefinition>
            <RowDefinition Height="100*">

            </RowDefinition>
            <RowDefinition Height="100*">

            </RowDefinition>
            <RowDefinition Height="100*">

            </RowDefinition>
            <RowDefinition Height="100*">

            </RowDefinition>
            <RowDefinition Height="100*">

            </RowDefinition>
            <RowDefinition Height="100*">

            </RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock HorizontalAlignment="Center" FontWeight="Bold"> Patient Id</TextBlock>
            <TextBox  Text="{Binding PatientId}"></TextBox>
            <Button Content="Open Patient" Command="{Binding OpenPatientCommand}"></Button>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0">
            <TextBlock FontWeight="Bold" Margin="0" HorizontalAlignment="Center">Courses</TextBlock>
            <ComboBox  Margin="0" x:Name="CourseListBox" ItemsSource="{Binding Courses}" SelectedItem="{Binding SelectedCourse, Mode=TwoWay}">
                <ie:Interaction.Triggers>
                    <ie:EventTrigger EventName="SelectionChanged">
                        <ie:InvokeCommandAction Command="{Binding ViewCourseCommand}"/>
                    </ie:EventTrigger>
                </ie:Interaction.Triggers>
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Id}">

                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0">
            <TextBlock Margin="0" FontWeight="Bold" HorizontalAlignment="Center">Plans</TextBlock>
            <ComboBox Margin="0" ItemsSource="{Binding Plans}" SelectedItem="{Binding SelectedPlan, Mode=TwoWay}">
                <ie:Interaction.Triggers>
                    <ie:EventTrigger EventName="SelectionChanged">
                        <ie:InvokeCommandAction Command="{Binding OnPlanSelectCommand}"/>
                    </ie:EventTrigger>
                </ie:Interaction.Triggers>
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Id}">

                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

       
        
        <StackPanel Grid.Row="3">
            <TextBlock Margin="0" FontWeight="Bold" HorizontalAlignment="Center">Fields</TextBlock>
            <ScrollViewer Height="300">
                <ListBox Margin="8" ItemsSource="{Binding Beams}" SelectedItem="{Binding SelectedBeam, Mode=TwoWay}">
                    <ie:Interaction.Triggers>
                        <ie:EventTrigger EventName="SelectionChanged">
                            <ie:InvokeCommandAction Command="{Binding OnBeamSelectCommand}"/>
                        </ie:EventTrigger>
                    </ie:Interaction.Triggers>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Id}">

                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.Column="0" Margin="0" Grid.RowSpan="2">
            <ComboBox Margin="10" 
                      ItemsSource="{Binding DRMethods}" 
                      SelectedValuePath="Item1"
                      SelectedValue="{Binding SelectedMethod}"
                      IsSynchronizedWithCurrentItem="True">
                <ie:Interaction.Triggers>
                    <ie:EventTrigger EventName="SelectionChanged">
                        <ie:InvokeCommandAction Command="{Binding OnMethodSelectCommand}"/>
                    </ie:EventTrigger>
                </ie:Interaction.Triggers>
                <ComboBox.ItemTemplate >
                    <DataTemplate>
                        <ComboBoxItem  Content="{Binding Path=Item1}" IsEnabled="{Binding Path=Item2}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Margin="8" Content ="Edit DR for all fields" Command="{Binding EditDRCommand}">

            </Button>
        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="4">
            <Grid>
                <oxy:PlotView Margin="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Model="{Binding DRPlot}"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.Column="1" Margin="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <CheckBox Grid.Column="0" Grid.Row="0" Margin="5" Command="{Binding PreviewDRCommand}" IsChecked="{Binding PreviewDR}">
                    <TextBlock Background="Orange">Preview Doserate</TextBlock>
                </CheckBox>

                <CheckBox Grid.Column="0" Grid.Row="1" Margin="5" Command="{Binding PreviewGSCommand}" IsChecked="{Binding PreviewGS}">
                    <TextBlock Background="LightBlue">Gantry Speed Preview</TextBlock>
                </CheckBox>

                <CheckBox Grid.Column="0" Grid.Row="2" Margin="5" Command="{Binding PreviewdMUCommand}" IsChecked="{Binding PreviewdMU}">
                    <TextBlock>Delta MU Preview</TextBlock>
                </CheckBox>

                <CheckBox Grid.Column="1" Grid.Row="0" Margin="5" Command="{Binding PlotCurrentDRCommand}" IsChecked="{Binding CurrentDR}">
                    <TextBlock Background="Red">Current Doserate</TextBlock>
                </CheckBox>
                <CheckBox Grid.Column="1" Grid.Row="1" Margin="5" Command="{Binding PlotCurrentGSCommand}" IsChecked="{Binding CurrentGS}">
                    <TextBlock Background="#3283a8">Current Gantry Speed</TextBlock>
                </CheckBox>
                <CheckBox Grid.Column="1" Grid.Row="2" Margin="5" Command="{Binding PlotCurrentdMUCommand}" IsChecked="{Binding CurrentdMU}">
                    <TextBlock Background="Yellow">Current Delta MU</TextBlock>
                </CheckBox>

            </Grid>

        </StackPanel>
        <StackPanel Grid.Row="6" Grid.Column="1" Margin="0" Grid.RowSpan="2">
            <TextBlock Text="{Binding CreditText}"></TextBlock>
        </StackPanel>





        <StackPanel Grid.Column="2" Grid.Row="0" Grid.RowSpan="4">
            <oxy:PlotView Margin="8" Controller="{Binding TransController}"  Model="{Binding TransPlot}" Height="362"/>
        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="3" Grid.RowSpan="4">
            <oxy:PlotView HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Model="{Binding View1}" Height="300" Width="300"></oxy:PlotView>
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.Column="3" Grid.RowSpan="2" Margin="0,60,0,0">
            <oxy:PlotView HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Model="{Binding View2}" Height="300" Width="300"></oxy:PlotView>
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.Column="2" Grid.RowSpan="2" Margin="0,60,0,0">
            <oxy:PlotView HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Model="{Binding View3}" Height="300" Width="300"></oxy:PlotView>
        </StackPanel>


        <TextBlock Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="4" Background="PaleVioletRed" Margin="0,125,0,0" Grid.RowSpan="2">    
            <Label Margin="0">
                <Hyperlink Command="{Binding HyperlinkCmd}">Bound by the terms of the Varian LUSLA</Hyperlink>
            </Label>
            <!--Button Command="{Binding HyperlinkCmd}">View agreement</Button-->
            <Label Margin="0" Content="{Binding PostText}"/>
        </TextBlock>
    </Grid>
</metro:MetroWindow>
 